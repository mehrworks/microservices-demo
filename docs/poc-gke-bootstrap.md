# GKE bootstrap POC runbook

> **Status:** deprecated pointer stub  
> **Branch context:** `spike/dormant-scaffold-v2`

This file is intentionally kept as a **dormant reference stub** rather than being removed.

The canonical branch guide and bootstrap instructions now live in:

- [`docs/dormant-scaffold.md`](/docs/dormant-scaffold.md)

## Why this file still exists

On this scaffold branch, the preferred cleanup style is to **comment out / deprecate / point** rather than delete historical docs outright unless deletion is explicitly requested.

So this file remains as:

- a familiar pointer for anyone looking for the old standalone GKE POC runbook
- a reminder that the bootstrap proof flow was consolidated into the single canonical branch guide

## What to use instead

Open [`docs/dormant-scaffold.md`](/docs/dormant-scaffold.md) and use the **Bootstrap proof (GKE)** section there.

That guide now covers:

- current branch operating model
- active vs dormant surfaces
- reactivation rules
- GKE bootstrap proof steps
- cleanup guidance

## Update rule

If the bootstrap proof process changes, update:

1. [`docs/dormant-scaffold.md`](/docs/dormant-scaffold.md)
2. this pointer stub only if the link or guidance summary needs adjustment
