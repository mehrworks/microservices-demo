# yaml-language-server: $schema=./schema/sandbox-public.schema.json

project_id: REPLACE_WITH_PROJECT_ID

artifact_registry:
  create: true

cluster:
  create: true

hardening:
  private_nodes_enabled: false
  master_ipv4_cidr: null
  cloud_nat_enabled: false
  public_load_balancer_enabled: true
